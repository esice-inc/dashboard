import { combineReducers } from 'redux';

const payments = {
};


const actions = {
};

function paymentsReducer(state = sales, action) {
  switch(action.type) {
  }
}

const rootReducer = combineReducers({
});

export {
  actions,
  rootReducer,
};
